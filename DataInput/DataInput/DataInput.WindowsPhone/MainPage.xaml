<Page
    x:Class="DataInput.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DataInput"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.BottomAppBar>
        <CommandBar IsOpen="True" IsSticky="True">
            <AppBarButton Name="New" Icon="Page" Label="New" Click="New_Click"/>
            <AppBarButton Name="Open" Icon="Folder" Label="Open" Click="Open_Click"/>
            <AppBarButton Name="Save" Icon="Save" Label="Save" Click="Save_Click"/>
        </CommandBar>
    </Page.BottomAppBar>
    <Grid>
        <StackPanel Margin="40">
            <TextBox Name="Email" PlaceholderText="Email" InputScope="EmailSmtpAddress"/>
            <TextBox Name="Website" PlaceholderText="Website" InputScope="Url"/>
            <TextBox Name="Telephone" PlaceholderText="Telephone" InputScope="TelephoneNumber"/>
        </StackPanel>
    </Grid>
</Page>
    